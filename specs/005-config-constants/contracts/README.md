# Contracts: Centralized Game Configuration

No API contracts for this feature - it's a code refactoring that doesn't change any API interfaces.

The configuration structure is documented in [data-model.md](../data-model.md).
